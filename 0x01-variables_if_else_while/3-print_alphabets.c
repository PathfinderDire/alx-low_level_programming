#include <stdio.h>

/**
 * main - starting of code
 *
 * Return:is Always 0 (Success)
 */

int main (void)
{
	int n=97;
	int p=65;
	
	while (97 <=  n <= 122)
	{ putchar(n);
		n++;
	};
	while (65 <= p <= 90)
	{ putchar(p);
		n++;
	};
	putchar ('\n');
	return (0);


}

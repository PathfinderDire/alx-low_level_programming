#include <stdio.h>

/**
 * main - starting of code
 *
 * Return:is Always 0 (Success)
 */

int main (void)
{
	int n = 97;
	int k = 65;
	
	while (n <= 122)
	{ putchar('n');
		n++;
	};
	while (k <= 90)
	{ putchar('k');
		n++;
	};
	putchar ('\n');
	return (0);


}

#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of string
 * @s: Array
 * Return: lenth of string
 */

int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);


}

#include <stdio.h>

/**
 * main: beginning of code
 *
 * Return:is always 0 (Success)
 */

int main(void)
{
	int n = 48;
	char letter = 'a';

	while (n <= 57)
	{ putchar('n');
		n++;
	};
       while (letter <= 'f')
	{ putchar(letter);
		letter++;
	};
	return (0);


}

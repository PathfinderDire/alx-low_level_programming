#include <stdio.h>

/**
 * main - starting of code
 *
 * Return:is Always 0 (Success)
 */

int main (void)
{
	int n=97;
	int p=65;
	
	while (n <= 122)
	{ putchar(n);
		n++;
	};
	while (p <= 90)
	{ putchar(n);
		n++;
	};
	putchar ('\n');
	return (0);


}

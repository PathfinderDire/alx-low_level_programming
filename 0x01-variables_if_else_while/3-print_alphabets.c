#include <stdio.h>

/**
 * main - starting of code
 *
 * Return:is Always 0 (Success)
 */

int main(void)
{
	int n = 97;
	int k = 90;
	while(n < 122)
        {putchar(n);
                n++;
        };
	while(k < 90)
	{putchar(k);
		k++;
	}
	;
	return (0);


}
